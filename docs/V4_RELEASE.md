# Project verification record

This compatibility entry point records the repository's complete recreation and verification history for readers who
arrive through an older link. It is intentionally not a feature guide; the feature and operator documentation lives in
the other pages in this directory.

The GitHub wiki is published from `docs/` by the repository workflow. Historical verification evidence includes commit
`eaf7628`; verify the commit in the repository before relying on it as current evidence.

The live contract covers specialties `system`, `hardware`, `process`, `memory`, `filesystem`, `network`, `packet`,
`wireless`, `bluetooth`, `usb`, `browser`, `registry`, `event_log`, `storage`, `encryption`, `media`, `ssh`,
`diagnostics`, `reporting`, and `integration`; modes `quick`, `balanced`, `standard`, `offline`, and `thorough`; and
MIME types `application/json`, `application/octet-stream`, `application/xml`, `application/zip`, `text/csv`,
`text/html`, `text/plain`, and `text/vnd.graphviz`.
